# Correction Change Control

This release invalidates all prior visual and live proof for the candidate-vision header, responsive navigation, Mission Window interaction, application-document URLs, terminology, generated PDFs, and deployment.

No earlier screenshot, page-count result, source assertion, or deployment receipt may be reused as completion evidence. The corrected source must pass the current source, rendered-browser, generated-PDF, main-branch, and live-deployment gates before the campaign returns to complete.
