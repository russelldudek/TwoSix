# Publication Verification Record

Live candidate vision: https://russelldudek.github.io/TwoSix/

Canonical publication branch: `main`

## Five-page entry-plan release

The corrected 120-Day Entry Plan contains one cover and four substantive 30-day phase pages. The generated PDF contract is exactly five US Letter pages. The release must preserve the complete candidate-vision URL, the native PDF download, deliberate screen reflow, printable footer clearance, page-use discipline, and the depth topics enforced by source regression.

## Release workflow

The final publication must:

1. run recursive source and confidentiality regression;
2. render the full route and five-viewport matrix;
3. generate every printable document from HTML;
4. verify exact PDF contracts `2 / 1 / 4 / 5 / 2` and content boundaries;
5. assemble the complete static release;
6. deploy that exact source commit to GitHub Pages;
7. compare the deployed routes, styles, script, logo, and PDFs byte-for-byte with the release source;
8. exercise the public desktop, mobile, reduced-motion, scenario, reciprocal-document, and five-page entry-plan flows; and
9. persist generated PDFs plus `deployment-status.json` only after verification.

A successful source commit or deployment commit alone is not live proof. Completion requires `deployment-status.json` to identify the final publication source, the public URL, `live_audit: passed`, and the inspectable live-audit run.
