# Release Gate Status

Current state: release candidate. Completion is computed from the exact-live receipt in `deployment-status.json`.

Passed:

- navigation uniqueness and responsive interaction safeguards;
- atomic scenario controller, keyboard behavior, and reduced motion;
- complete candidate-vision URLs and native document downloads;
- five-page entry-plan source contract;
- four phase-specific 30-day sheets;
- stakeholder, decision-rights, hypothesis, evidence, coaching, economics, cadence, and next-quarter depth;
- recursive confidentiality and terminology scan;
- five-viewport rendered browser audit;
- screen overflow, print footer geometry, and page-use audit;
- exact generated PDF pagination `2 / 1 / 4 / 5 / 2`;
- generated-document text and metadata audit.

Quality evidence: workflow runs `29837523440` and `29837760040`.

Completion rule:

- `deployment-status.json` must identify the final publication source;
- the receipt must record `live_audit: passed` and the inspectable live-audit run;
- the deployed entry-plan HTML, `entry-plan-2026.css`, and five-page PDF must match the final source;
- the public entry-plan route, phase architecture, responsive screen behavior, and native PDF download must pass.

If any condition is absent or stale, the campaign state is building regardless of source quality.
